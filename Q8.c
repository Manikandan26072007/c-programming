#include <stdio.h>

int main()
{
    int decimalNumber;
    printf("Enter a decimal number: ");
     scanf("%d", &decimalNumber);
     printf("The decimal number %d in octal format is: %o\n", decimalNumber, decimalNumber);
     return 0;
 }
